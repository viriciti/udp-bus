{
  "name": "@viriciti/udp-bus",
  "version": "1.1.4",
  "description": "A comunication bus that uses UDP to send/receive messages",
  "main": "build/index.js",
  "scripts": {
    "compile": "coffee --output build --compile src",
    "test": "mocha --require coffeescript/register test/* --exit"
  },
  "files": [
    "build"
  ],
  "author": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/viriciti/udp-bus.git"
  },
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "coffeescript": "^2.3.1",
    "mocha": "^10.2.0"
  }
}
